

// import { userRole } from '@/constants/userRole'
// import { TSidebarItem, TUserPath } from '@/types';




// export const sidebarItemsGenerator = (paths: TUserPath[], role: string): TSidebarItem[] => {
//     return paths.map(item => ({
//         ...item,
//         label: `${role === userRole.ADMIN ? 'Admin' : 'Customer'} - ${item.name}`,
//     }));
// }

